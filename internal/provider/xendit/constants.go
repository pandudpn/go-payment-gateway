package xendit

const (
	// ProviderName is the name of the Xendit provider
	ProviderName = "xendit"

	// API URLs (Xendit uses same URL for both sandbox and production)
	sandboxURL    = "https://api.xendit.co"
	productionURL = "https://api.xendit.co"
)
